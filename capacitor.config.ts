import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.geo.geodyssey',
  appName: 'geodyssey',
  webDir: 'dist/geodyssey/browser'
};

export default config;
